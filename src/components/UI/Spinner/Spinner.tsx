import React from 'react';

import styles from 'components/UI/Spinner/Spinner.scss';

const Spinner = (): JSX.Element => (
    <div className={styles.loader}>Loading...</div>
);

export default Spinner;
